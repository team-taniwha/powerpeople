<script>
  var serverFlashcards = <%= raw(@flashcards.to_json(include: { staff_member: { methods: [:joined_powershop_recently?, :first_name] }})) %>;

  window.flashcards = serverFlashcards;
  window.authenticity_token = "<%= form_authenticity_token %>";
  window.newStaffMemberImagePath = "<%= image_path('new.png') %>";
</script>

<div id='app'></div>
